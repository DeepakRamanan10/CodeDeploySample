version: 0.0
os: linux
files:
  - source: /deploy
    destination: /home/ec2-user
hooks:
  BeforeInstall:
    - location: deploy/deploy.sh
      timeout: 300
      runas: ec2-user
  AfterInstall:
    - location: deploy/restart.sh
      timeout: 300
      runas: ec2-user
